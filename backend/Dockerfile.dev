FROM node:18-alpine

# ffmpeg/ffprobeをインストール（25MB超音声ファイル分割処理用）
RUN apk add --no-cache ffmpeg

# 作業ディレクトリの設定
WORKDIR /app

# パッケージファイルをコピー
COPY package*.json ./

# 依存関係のインストール
RUN npm install

# アプリケーションコードをコピー
COPY . .

# ポートを公開
EXPOSE 8000

# 開発モードで起動
CMD ["npm", "run", "dev"]